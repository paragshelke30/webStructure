(function () {
  'use strict';

  angular
    .module('web-admin')
    .config(configure);

  configure.$inject = ['$mdThemingProvider', '$provide'];

  function configure($mdThemingProvider, $provide) {}
})();
